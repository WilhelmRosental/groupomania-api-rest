{
    "name": "@groupomania/shared",
    "version": "1.0.0",
    "description": "Shared utilities for Groupomania microservices",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "clean": "node -e \"const fs = require('fs'); if (fs.existsSync('dist')) fs.rmSync('dist', {recursive: true, force: true});\""
    },
    "dependencies": {
        "pino": "^9.7.0",
        "pino-pretty": "^13.0.0",
        "zod": "^4.0.10"
    },
    "devDependencies": {
        "typescript": "^5.8.3",
        "@types/node": "^24.1.0",
        "fastify": "^5.4.0"
    },
    "peerDependencies": {
        "fastify": "^5.4.0"
    }
}